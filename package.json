{
  "name": "yolocalc",
  "version": "1.0.0",
  "description": "YoloCalc — Retire with memories, not just money.",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'Static site — no build needed'"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
